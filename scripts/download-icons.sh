#!/usr/bin/env bash
# This script is deprecated.
# Icons are now embedded as inline SVGs in the React components, so
# downloading raster icon files is no longer required. Remove this
# script and the files in public/assets/icons/ if you don't need them.

exit 0
